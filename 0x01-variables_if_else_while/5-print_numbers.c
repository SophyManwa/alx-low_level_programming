#include <stdio.h>

/**
 * main - Prints single digit numbers of base 10.
 * Return: 0.
 */

int main(void)
{
	int s;
	/*for loop execution*/
	for (s = 0; s < 10; s++)
		printf("%d\n", s);
	return (0);
}
