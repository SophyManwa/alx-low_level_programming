#include <stdio.h>
/**
 * main - prints the sum of all multiples of3 or 5 up to 1024
 *
 * Return: Always (0)
 */
int main(void)
{
	int s, k = 0;

	while (s < 1024)
	{
		if ((s % 3 == 0) || (i % 5 == 0))
		{
			z += i;
		}
		i++;
	}
	printf("%d\n", z);
	return (0);
}
